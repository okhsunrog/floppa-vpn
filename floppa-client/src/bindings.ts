/* eslint-disable */
// @ts-nocheck
// This file was generated by [tauri-specta](https://github.com/oscartbeaumont/tauri-specta). Do not edit this file manually.

/** user-defined commands **/


export const commands = {
/**
 * Get the persistent device UUID (created on first call)
 */
async getDeviceId() : Promise<Result<string, string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("get_device_id") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Get the device name (Android: manufacturer+model, desktop: hostname)
 */
async getDeviceName() : Promise<string> {
    return await TAURI_INVOKE("get_device_name");
},
/**
 * Parse a WireGuard config string, set it as the active config, and persist it.
 */
async setActiveConfig(configStr: string) : Promise<Result<null, string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_active_config", { configStr }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Clear the active config from memory and delete persisted config. Disconnects first if connected.
 */
async clearConfig() : Promise<Result<null, string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("clear_config") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Load persisted WireGuard config into memory (called on startup).
 */
async loadSavedConfig() : Promise<Result<boolean, string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("load_saved_config") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Get current saved config (without private key for security)
 */
async getConfig() : Promise<Result<WgConfigSafe | null, string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("get_config") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Connect to VPN
 */
async connect(splitMode: SplitMode | null, selectedApps: string[] | null) : Promise<Result<null, string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("connect", { splitMode, selectedApps }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Disconnect from VPN
 */
async disconnect() : Promise<Result<null, string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("disconnect") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Get current connection info with live traffic stats
 */
async getConnectionInfo() : Promise<Result<ConnectionInfo, string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("get_connection_info") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Get list of installed apps for split tunneling (Android only)
 */
async getInstalledApps() : Promise<Result<AppInfo[], string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("get_installed_apps") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Get safe area insets (status bar, nav bar heights) in dp
 */
async getSafeAreaInsets() : Promise<Result<SafeAreaInsets, string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("get_safe_area_insets") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
}
}

/** user-defined events **/



/** user-defined constants **/



/** user-defined types **/

/**
 * Information about an installed app (for split tunneling UI)
 */
export type AppInfo = { package_name: string; label: string; is_system: boolean; icon: string | null }
/**
 * Connection information
 */
export type ConnectionInfo = { status: ConnectionStatus; server_endpoint: string | null; assigned_ip: string | null; connected_at: number | null; last_handshake: number | null; latency_ms: number | null; stats: TrafficStats }
/**
 * Connection status enum
 */
export type ConnectionStatus = "disconnected" | "connecting" | "connected" | "disconnecting" | "error"
/**
 * Safe area insets (status bar, nav bar) in dp
 */
export type SafeAreaInsets = { top: number; bottom: number }
/**
 * Split tunneling mode
 */
export type SplitMode = "all" | "include" | "exclude"
/**
 * Traffic statistics
 */
export type TrafficStats = { tx_bytes: number; rx_bytes: number; tx_bytes_per_sec: number; rx_bytes_per_sec: number }
/**
 * Safe config info (no private key)
 */
export type WgConfigSafe = { address: string; dns: string | null; peer_endpoint: string; allowed_ips: string; mtu: number | null }

/** tauri-specta globals **/

import {
	invoke as TAURI_INVOKE,
	Channel as TAURI_CHANNEL,
} from "@tauri-apps/api/core";
import * as TAURI_API_EVENT from "@tauri-apps/api/event";
import { type WebviewWindow as __WebviewWindow__ } from "@tauri-apps/api/webviewWindow";

type __EventObj__<T> = {
	listen: (
		cb: TAURI_API_EVENT.EventCallback<T>,
	) => ReturnType<typeof TAURI_API_EVENT.listen<T>>;
	once: (
		cb: TAURI_API_EVENT.EventCallback<T>,
	) => ReturnType<typeof TAURI_API_EVENT.once<T>>;
	emit: null extends T
		? (payload?: T) => ReturnType<typeof TAURI_API_EVENT.emit>
		: (payload: T) => ReturnType<typeof TAURI_API_EVENT.emit>;
};

export type Result<T, E> =
	| { status: "ok"; data: T }
	| { status: "error"; error: E };

function __makeEvents__<T extends Record<string, any>>(
	mappings: Record<keyof T, string>,
) {
	return new Proxy(
		{} as unknown as {
			[K in keyof T]: __EventObj__<T[K]> & {
				(handle: __WebviewWindow__): __EventObj__<T[K]>;
			};
		},
		{
			get: (_, event) => {
				const name = mappings[event as keyof T];

				return new Proxy((() => {}) as any, {
					apply: (_, __, [window]: [__WebviewWindow__]) => ({
						listen: (arg: any) => window.listen(name, arg),
						once: (arg: any) => window.once(name, arg),
						emit: (arg: any) => window.emit(name, arg),
					}),
					get: (_, command: keyof __EventObj__<any>) => {
						switch (command) {
							case "listen":
								return (arg: any) => TAURI_API_EVENT.listen(name, arg);
							case "once":
								return (arg: any) => TAURI_API_EVENT.once(name, arg);
							case "emit":
								return (arg: any) => TAURI_API_EVENT.emit(name, arg);
						}
					},
				});
			},
		},
	);
}
